[project]
name = "mcode"
version = "0.1.0"
description = "Терминальная агентная система с поддержкой различных LLM провайдеров и MCP протокола"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "click>=8.1.8",
    "prompt-toolkit>=3.0.51",
    "pydantic>=2.11.4",
    "pydantic-ai>=0.1.9",
    "python-dotenv>=1.1.0",
    "rich>=14.0.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[project.scripts]
mcode = "src.cli.main:main"
mcode-demo = "src.llms.main:main"

[tool.uv]
dev-dependencies = [
    "ruff>=0.8.4",
    "black>=24.10.0",
    "pytest>=8.3.4",
]

[tool.ruff]
line-length = 88
target-version = "py313"
src = ["src"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]
ignore = ["E501"]  # line too long

[tool.black]
line-length = 88
target-version = ["py313"]
include = '\.pyi?$'
extend-exclude = '''
/(
    \.git
  | \.venv
  | build
  | dist
)/
'''
